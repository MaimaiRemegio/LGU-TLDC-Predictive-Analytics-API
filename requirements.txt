# Minimal runtime dependencies for LGU-TLDC Predictive Analytics API.
# Generated from project imports only (not pip freeze).
# Compatible with the local environment that successfully runs this API.

# ASGI web framework and server
fastapi==0.138.2
uvicorn==0.49.0

# Request/response validation (imported directly in routes)
pydantic==2.13.4

# Data processing (CSV loading, feature matrices, historical stats)
pandas==3.0.3
numpy==2.5.0

# Model persistence and Random Forest inference
# scikit-learn is required at runtime to unpickle RandomForestClassifier
# and LabelEncoder objects from trained_models/*.pkl
scikit-learn==1.9.0
joblib==1.5.3

# ARIMA forecast models are loaded via joblib and call .forecast();
# statsmodels must be present to deserialize and run those models.
statsmodels==0.14.6
